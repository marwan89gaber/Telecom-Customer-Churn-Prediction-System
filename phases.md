Phase 1 — Foundation & setup. Scaffold the entire repo before writing a single model. Folder structure, Docker skeleton, PostgreSQL container, requirements.txt, .gitignore, README shell. Use the IBM Telco Churn dataset — 7k rows, 21 features, publicly available, Vodafone-plausible.
Phase 2 — Data engineering pipeline. Build a real ETL script, not notebook cells. Raw CSV → clean → transform → PostgreSQL. Then run a PySpark batch job on it to demonstrate big data thinking even on small data. This is what separates you from notebook-only candidates.
Phase 3 — EDA & feature engineering. Go beyond charts. Explain what the churn rate by tenure bucket means for retention spend. Engineer signal-heavy features. Document class imbalance and how you handle it. Export everything to a screenshots folder.
Phase 4 — ML modelling & evaluation. Run 4 models, compare honestly, pick the winner with justification. Tune properly. Use recall as your primary metric — false negatives cost money in churn. Add SHAP. This is your ML credibility section.
Phase 5 — Batch prediction pipeline. A Spark job that reads the customer table nightly, scores everyone, writes risk tiers back to the database. Add a retraining trigger based on PSI drift. Show one Airflow DAG — even a screenshot. This proves engineering maturity.
Phase 6 — Real-time streaming. A Kafka producer simulating live events feeding Spark Structured Streaming, with high-risk alerts piped to a dedicated topic. Everything Dockerized. This is the section that makes Vodafone's data engineering team pay attention.
Phase 7 — API, dashboard & deployment. Flask with three endpoints. Docker-compose ties all 6 services together. Power BI dashboard with business-facing KPIs and SHAP top drivers. Swagger docs. Architecture diagram. This is what goes in front of a hiring manager.
Phase 8 — Documentation & portfolio packaging. Most people skip this and it shows. Write the README like a product page. Document every challenge and how you fixed it — it's what proves the project is real. Tag a release. Record a 2-minute Loom. That alone puts you in the top 5% of portfolios.
