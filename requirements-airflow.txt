# Airflow container dependencies — only what DAG tasks actually import.
# Pinned to match Apache Airflow 2.9.3 constraints for Python 3.12
# Do not use pandas==2.2.2 or sqlalchemy==2.0.30 — they conflict with Airflow

pandas==2.1.4
numpy==1.26.4
scikit-learn==1.4.2
xgboost==2.0.3
lightgbm==4.3.0
joblib==1.4.2
psycopg2-binary==2.9.9
sqlalchemy==1.4.52
python-dotenv==1.0.1
loguru==0.7.2
